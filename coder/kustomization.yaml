# Fleet entry point - single kustomization for prd-apps
# Path "coder" - Fleet runs kustomize build here
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: coder

resources:
  - base

commonLabels:
  managed-by: gitops
  cluster: prd-apps
